#!/bin/bash

# Перевірка, чи передано назву гілки
if [ $# -eq 0 ]; then
    echo "Потрібно вказати назву гілки."
    exit 1
fi

# Перенесення зафіксованих змін на віддалений сервер
git push origin "$1"
